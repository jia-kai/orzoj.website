data and source transfering from server to website
default settings such as:
	admin's account
	a+b
	plang wlang
	add default theme to database

set time format


Problem page: discuss

code editor

status filter:
	an optional radio box contains code and id supposed to be setted


users.php: user_get_user_amount

题目打钩


删除久远的代码

how about changing all recursive functions to manual stack?
	-- i guess doing so will make it much slower, no experiment now (jk)

is pomo still being used?

limit the max length of nickname?

the time2str function returns a too-long string..

add index.html

add some default expressions to the rich text editor.

