data and source transfering from server to website
default settings such as:
	admin's account
	a+b
	plang wlang
	add default theme to database

set time format

contents/themes/default/ajax/avatar_browser.php:102
	Go to page: => Go to page: %s where %s is the inputbox.

t_get_link : in_HTML

includes/message.php:75 单词拼写错误

注册后自动登录

注册时检查密码是否相符
检查邮箱地址合法性

problem not permitted returns NULL

There still seems something wrong with problem select by group
